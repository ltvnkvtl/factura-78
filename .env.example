# Public URL of the Cloudflare Worker that proxies lead-form submissions to Telegram.
# Deploy worker/ first (see worker/README.md), then paste the URL here.
# Pre-pended with PUBLIC_ so Astro exposes it to client bundles.
PUBLIC_LEAD_FORM_URL=https://factura78-lead.factura78.workers.dev
